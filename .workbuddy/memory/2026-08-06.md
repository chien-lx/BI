# 2026-08-06

- 修改仪表盘配置页（`src/prototypes/data-analysis-engine/pages/dashboard/config/index.tsx`）：将图表配置面板底部的「取消 / 保存」两个按钮替换为单个「数据更新」按钮，点击时保存当前图表配置并触发查询刷新。
- 同文件：移除「数据更新」区域内的「立即刷新」按钮以及下方的「查询」按钮，仅保留自动刷新间隔设置。
- 同文件：样式 Tab 中「背景、边框、提示、表头、单元格、总计、功能设置、滚动设置」等折叠面板默认展开并显示完整配置项；补充了对应 state 字段（内边距、圆角、渐变、文字颜色、对齐、行高、自动滚动等）。
- 数据大屏列表页（`src/prototypes/data-analysis-engine/pages/DataScreenPage.tsx`）：「查看」改为跳转预览页，`操作列增加「配置」按钮跳转配置页；「新建大屏」也直接进入配置页。
- 新增数据大屏配置页（`src/prototypes/data-analysis-engine/pages/data-screen/config/index.tsx`）：三栏布局（组件库/画布/属性），支持添加组件、图层管理、位置尺寸编辑、数据绑定、交互事件、动画、画布设置。
- 新增数据大屏预览页（`src/prototypes/data-analysis-engine/pages/data-screen/preview/index.tsx`）：按大屏分辨率比例渲染所有组件，深色科技风背景。
- 更新路由（`index.tsx`）和 Layout 类型（`components/Layout.tsx`），注册 `data-screen-config` / `data-screen-preview` 为全屏页面并高亮「数据大屏」父菜单。
- 更新 `data/mockData.ts`：为 `DataScreenItem` 增加 `bgColor`、`bgImage`、`components` 字段，并给 15 条大屏数据补充了默认组件布局。
- 在 `style.css` 追加 `.de-screen-tab`、`.de-screen-comp-btn` 编辑器专用样式。
- 对齐数据大屏配置页 UI 到仪表盘配置页（`data-screen/config/index.tsx`）：顶部工具栏改为「返回+名称+撤销/重做 | 中间组件插入工具栏 | 缩放+预览/保存」三段式，与仪表盘配置页一致；画布空状态增加提示。
- 仪表盘配置页组件支持设置大小与位置（`dashboard/config/index.tsx`）：启用 grid 显式定位（`gridColumn`/`gridRow` 用 x/y/w/h），画布支持拖动卡片移动、拖右下角缩放；右侧「数据」Tab 新增「位置与尺寸」区（列宽/行高/列起始/行起始 + 自动排列开关）。`DashboardChart` 已含 x/y/w/h 字段，无需改类型。
- 验证说明：`npm run typecheck` 中 `data-screen/config`、`dashboard/config` 两个文件无新增类型错误；直接 curl 模块路径报 500 属探针假象（已知可运行的 preview 模块同样 500，因 `@/` 别名在直接 URL 请求下解析上下文不同），应用内实际加载正常。
- 仪表盘配置页（`dashboard/config/index.tsx`）画布从 Grid 布局改为自由布局（绝对定位像素值）：使用 1200×800 画布，组件按 `x/y/w/h` 像素定位；支持拖动标题栏移动、拖右下角缩放；右侧「位置与尺寸」改为 X/Y/宽度/高度输入框。同步修改 `dashboard/preview/index.tsx` 预览页也按自由布局渲染。
