# 2026-08-11 工作日志

- 调整数据大屏配置页（`src/prototypes/data-analysis-engine/pages/data-screen/config/index.tsx`）：
  - 移除顶部导航栏中的组件插入工具栏，因左侧「组件」面板已承担该能力。
  - 移除顶部导航栏右侧的屏幕尺寸单独展示（`3840 × 1080`），分辨率已在下拉选择中体现。
- 数据大屏配置页右侧「数据/属性」Tab 对齐仪表盘配置页：
  - 「数据」Tab 增加图表名称、类型、位置尺寸、数据集、维度/指标 pill、数据过滤、数据更新。
  - 「属性」Tab 增加基础样式、标题、背景、边框、提示、表头、单元格、总计、功能设置、滚动设置、条件样式等折叠面板。
  - 配置项全部受控，并保存到每个组件的 `config` 字段中；画布组件会即时读取配置生效。
- 修复数据大屏配置页顶部「预览」按钮：改为 `window.open` 新标签页打开 `data-screen-preview` 页面，URL 与列表页「查看」跳转一致（`${pathname}#page=data-screen-preview&screenId=${config.id}`）。
- 增强用户管理页面（`src/prototypes/data-analysis-engine/pages/UserManagePage.tsx`）：
  - 操作列新增：查看详情、编辑、设置数据权限、数据透视、禁用/恢复禁用、删除。
  - 用户详情 Drawer：展示基本信息、角色信息、角色所拥有的菜单权限。
  - 编辑用户信息 Drawer：可编辑姓名、邮箱、手机号、角色、部门、职位、状态。
  - 设置数据权限 Drawer：针对数据源、数据集、图表、报表、仪表盘、数据大屏分别设置查看/管理权限。
  - 数据透视 Drawer：汇总展示菜单权限和对应数据内容的查看/管理权限。
  - 扩展 `mockData.ts` 中 `UserItem` 类型及 mock 数据，补充角色菜单权限。

- 用户管理页面二次迭代（响应截图反馈）：
  - `mockData.ts`：新增 `ResourcePermission` 资源级权限类型（按数据源/数据集/图表/报表/仪表盘/数据大屏的具体资源项设置 view/manage），并用 `buildResourcePermissions(roleId)` 为每个用户生成 `resourcePermissions`；导出 `resourcePermissionGroups`。
  - 禁用/恢复：列表改用 `usersState` 本地 state，`toggleStatus` 直接更新 state，列表状态标签实时同步（不再只是 alert）。
  - 用户详情菜单权限：明确区分「查看 / 管理」两列，用绿色 ✓ 标签 / 灰色 — 标签展示。
  - 设置数据权限：改为资源级设置（按 6 类资源分组列出具体 A/B/C 资源），每项单独勾选查看、管理；规则：管理包含查看（勾管理自动勾查看，取消查看自动取消管理；查看被管理占用时禁用）。
  - 数据透视：同步改为「菜单权限 + 各资源分组的具体资源查看/管理权限」展示。
  - 删除用户也会从 `usersState` 中真正移除。typecheck 通过，dev 服务器 51720 可预览。

- 用户管理页面第三次迭代（响应截图反馈）：
  - `mockData.ts`：新增 `menuTree`（按侧边栏分组的所有菜单）和 `menuToResourceType`（菜单与资源类型映射），为权限树和血缘图提供统一骨架。
  - 用户详情 / 编辑用户：菜单权限改为「解锁 / 加锁」图标展示，有权限显示蓝色解锁，无权限显示灰色加锁，不再区分查看/管理；编辑用户时选择角色后立即展示该角色的菜单权限。
  - 设置数据权限：改为左侧菜单目录树 + 右侧对应数据内容面板的布局；右侧增加搜索框、全部查看 / 全部管理 / 全部取消批量操作；资源较多时右侧表格使用 `maxHeight` 滚动；管理权限仍包含查看权限。
  - 数据透视：改为数据血缘图样式——左侧用户节点，中间菜单节点（可点击），右侧展示所选菜单下具有权限的数据内容；点击菜单展开对应资源权限。

- 修复 React hooks 报错：用户管理页「数据透视」渲染函数内调用 `useMemo` 导致条件渲染 hook 数量不一致；改为普通内联计算后恢复。
- 用户管理 Drawer 加宽：「设置用户数据权限」和「用户权限数据透视」两个 Drawer 宽度从默认 560px 改为 720px。
- 重写角色管理页面（`src/prototypes/data-analysis-engine/pages/RoleManagePage.tsx`）：
  - `mockData.ts` 中 `RoleItem` 增加 `status` 字段（active/inactive），mock 角色补充状态（访客为 inactive）。
  - 操作列扩展：查看详情、编辑、数据透视、禁用角色 / 恢复角色、删除。
  - 禁用/恢复状态直接更新本地 `rolesState`，列表状态标签同步变化。
  - 删除角色会从 `rolesState` 中移除。
  - 角色详情 Drawer：展示基本信息、菜单权限（解锁/加锁图标）、角色下的用户列表。
  - 编辑/新建角色 Drawer：可编辑角色名称、描述、状态，并通过解锁/加锁编辑菜单权限。
  - 数据透视 Drawer：展示「角色 → 用户」血缘图，列出该角色下所有用户。
  - typecheck 通过，dev 服务器 51720 可预览。

- 角色/用户管理菜单权限树形化 + 角色绑定用户：
  - 新增共享组件 `src/prototypes/data-analysis-engine/components/MenuPermissionTree.tsx`，将分组菜单权限改为可折叠树形结构（分组作为父节点，子菜单可展开/收起），父节点显示全解锁 / 部分解锁 / 全加锁状态。
  - `UserManagePage.tsx` 和 `RoleManagePage.tsx` 的「详情/编辑」菜单权限均改用该共享组件，原平铺实现删除。
  - `RoleManagePage.tsx` 增加「绑定用户」按钮及 Drawer：左右双栏展示「未绑定用户」和「已绑定用户」，支持搜索、全选/取消全选、批量绑定、批量取消绑定；已绑定用户单行也可点击「取消绑定」；绑定/解绑后同步更新 `usersState` 和角色的 `userCount`。

- 美化数据透视左侧节点：
  - 新增共享组件 `src/prototypes/data-analysis-engine/components/PerspectiveNode.tsx`，使用卡片式节点（顶部渐变图标区 + 标题 + 副标题 + 圆角阴影）替代原来的蓝色圆形文字节点。
  - `RoleManagePage.tsx` 数据透视中「角色」节点改用 `PerspectiveNode`，并增加指向右侧用户列表的连接线和箭头。
  - `UserManagePage.tsx` 数据透视中「用户」节点改用 `PerspectiveNode`，并增加指向右侧菜单列表的连接线和箭头。
  - typecheck 通过，dev 服务器 51720 可预览。

- 二次美化透视节点：
  - `PerspectiveNode.tsx` 改为全渐变背景卡片，减少上下留白；使用内联 SVG 数据 URI 添加科技感装饰图案（圆环、虚线、点阵）。
  - 图标区域放大为磨砂玻璃效果圆形徽章，标题加粗并增加文字阴影，副标题改为半透明胶囊徽标。
  - 顶部加入柔光高光效果，整体视觉更饱满。

- 三次美化透视节点（按反馈简化）：
  - `PerspectiveNode.tsx` 改回白底卡片（`#fff`），文字改为黑色（`var(--dae-ink)`），副标题改为浅灰胶囊。
  - 图标圆形徽章使用浅色渐变底（`color + 透明`）配主色图标，去掉重渐变与 SVG 装饰图案。
  - 角色节点图标由 `Shield` 改为 `Users`（人员图标），用户节点图标由 `Users` 改为 `User`（人员图标）。

- 操作日志页面（`src/prototypes/data-analysis-engine/pages/OperationLogPage.tsx`）全面重构 + 导出 Excel：
  - `mockData.ts`：扩展 `OperationLog` 类型，新增 `account`、`menuId`、`actionType`（`OperationActionType`）、`before`、`after` 字段；补充覆盖多模块的 mock 日志（数据源/数据集/图表/仪表盘/用户管理/数据大屏/自助取数/报表），`menuId` 与 `menuTree` 的 id 完全对齐。
  - 左侧菜单目录树：按 `menuTree` 分组展示各菜单，点击按 `menuId` 过滤，并带数量角标。
  - 顶部筛选栏：人员名称、账号、操作类型（下拉）、开始/结束时间（date），支持重置。
  - 表格「详情」操作打开 Drawer，展示基本信息 + 「变更内容」（变更前 JSON / 变更后 JSON 两块 pre）。
  - 导出 Excel：安装 `xlsx` 到隔离 node workspace（`/Users/study/.workbuddy/binaries/node/workspace/node_modules/xlsx`），并在 `vite.config.ts` 增加 `xlsx` 的 resolve alias（指向 xlsx.mjs）与 optimizeDeps；`handleExport` 用 `XLSX.writeFile` 导出当前筛选结果为 `.xlsx`，含变更前后 JSON 字段。
  - typecheck 通过；dev 服务器 51720 运行；已验证 OperationLogPage 模块与 xlsx 依赖均能被 Vite 正常转换（HTTP 200）。

- 操作日志页二次迭代（响应反馈）：
  - 移除左侧菜单目录树每个菜单后的数量角标（"全部菜单" 也无数字）。
  - 新增 `menuActionTypeMap`：为每个菜单定义其可执行的操作类型枚举（如数据源=创建/更新/删除，自助取数=下载/导出，仪表盘=创建/更新/删除/发布），不再把全部操作类型共用到每个菜单。
  - 操作类型下拉框改为根据当前选中的菜单动态显示允许的操作；点击具体菜单时自动重置操作类型筛选（避免选中无效值），"全部菜单" 时显示所有涉及的操作类型。
  - 美化左侧菜单目录树层级：
    - 一级目录（分组）使用 `Folder` 图标 + 灰底 + 灰色加粗文字，明确为不可点击的父目录。
    - 二级菜单项相对父目录缩进，左侧增加竖向引导线与横向连接短线，hover 时有浅色背景，指针光标表示可点击。
  - typecheck 通过，Vite 转换验证 200。

- 时间查询组件交互增强（响应反馈）：
  - 新增共享组件 `src/prototypes/data-analysis-engine/components/DatePicker.tsx`，封装可点击的日期选择器：左侧显示 `Calendar` 图标，中间展示已选日期或占位文字，点击任意区域调用原生 `input.showPicker()` 弹出浏览器日期选择框；选择后通过 `onChange` 回传 `YYYY-MM-DD` 值；支持 focus 状态的边框高亮。
  - `OperationLogPage.tsx` 顶部筛选栏的「开始时间」「结束时间」由原生 `<input type="date">` 改为 `DatePicker`，保持筛选逻辑不变；项目中目前仅操作日志页涉及时问查询，已统一处理。
  - typecheck 通过；dev 服务器已启动在 51721（原 51720 被占用）。

- 修复 DatePicker 在跨域 iframe 中的报错：原型运行时将页面嵌在跨域 iframe 中，原生 `input.showPicker()` 被浏览器安全策略拦截（`SecurityError: showPicker() called from cross-origin iframe`），运行时直接白屏报错。
  - 改用自实现的日历弹层：点击触发器切换 `open` 状态，弹出层内渲染「年/月 + 上/下月切换 + 周一到周日表头 + 日期网格」，点击日期调用 `onChange` 并关闭；新增已选日期右侧的清除按钮；通过 `document mousedown` 监听实现点击外部关闭；周一起始、今日用主色描边标记、选中日填充主色。
  - 不再依赖任何原生日期选择 API，彻底规避跨域 iframe 限制；typecheck 通过（其余报错均为 DataExplorePage/dataset/report 既有问题，与本次无关）。

- 租户管理与全局租户切换（响应截图反馈）：
  - `mockData.ts` 扩展租户/用户模型：
    - `TenantItem` 增加 `adminUserId`（租户管理员）、`description`、`contactPhone`、`createdAt`。
    - `UserItem` 增加 `tenantMemberships`（支持一个用户属于多个租户，身份为 admin/member）和 `isSuperAdmin`（系统超级管理员）。
    - 为 mock 用户配置多租户关系：张三为超管且属于 T001/T002；李四同时属于 T002（admin）和 T003（member）；赵六同时属于 T004（admin）和 T001（member），演示「一个用户存在于多个租户空间」。
    - 新增 `currentUser`（固定为张三）、`getTenantAdminName`、`getTenantMembers`、`getCurrentUserTenants` 辅助函数。
  - 新增 `src/prototypes/data-analysis-engine/contexts/AuthContext.tsx`：管理当前登录用户、当前租户、可访问租户列表、`switchTenant`（持久化到 localStorage）、`logout`；在 `index.tsx` 中包裹应用。
  - `Layout.tsx` 顶部新增全局导航栏：
    - 右侧展示「当前租户」下拉（Building2 图标 + 租户名 + ChevronDown），下拉列出所有可访问租户，点击切换；当前租户带 Check 标记。
    - 右侧展示「当前账号」下拉（头像 + 姓名 + ChevronDown），下拉显示姓名/邮箱/角色，并提供「退出登录」。
    - 侧边栏菜单根据 `currentUser.isSuperAdmin` 过滤：仅超管可见「租户管理」，普通用户/租户管理员不显示。
  - 重写 `TenantManagePage.tsx`：
    - 列表支持搜索、分页；操作列增加「查看、编辑、禁用/恢复禁用、管理员、删除」。
    - 状态切换实时更新本地 `tenantsState`，禁用后状态标签变灰。
    - 详情 Drawer：基本信息网格 + 租户管理员卡片 + 租户成员列表（展示用户在该租户下的 admin/member 身份）。
    - 编辑/新建 Drawer：完整表单（名称、编码、联系人、电话、配额、到期时间、管理员、状态、描述），到期时间使用 `DatePicker`。
    - 设置管理员 Drawer：搜索用户列表，点击即可将该用户设为当前租户管理员；同步更新列表中的联系人。
    - 删除确认弹窗，确认后从 `tenantsState` 移除。
  - typecheck 通过（其余报错仍为 DataExplorePage/dataset/report 既有问题）。
