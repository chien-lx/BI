# 2026-08-19

## AI 助手面板：历史会话改为左侧边栏

- 按用户截图反馈，将 `AIAssistantWidget` 从「顶部 Header 下拉历史会话」改为「左侧固定会话列表 + 右侧消息区」的两栏布局。
- 面板宽度从 720px 扩到 900px，左侧边栏 240px。
- 左侧边栏包含：助手 Logo/名称、「开启新对话」按钮、会话搜索框、最近会话列表（支持置顶/重命名/删除）。
- 右侧 Header 简化：仅保留头像、助手名称/副标题、最小化/关闭按钮。
- 保留原有功能：可拖拽悬浮按钮、收起态、多会话管理、消息发送/手册检索。
- 涉及文件：`src/prototypes/data-analysis-engine/components/AIAssistant/AIAssistantWidget.tsx`。
- tsc 检查无新增错误，dev server 53817/51720 运行正常。

## AI 助手知识库：支持文档上传、在线预览/编辑、切分策略

- 调研 Coze/Dify 知识库设计：均支持本地文件上传、自动/自定义分段、Chunk 预览、向量/关键词检索。
- 扩展 `assistantManual.ts` 数据模型：新增 `sourceType`、`sourceName`、`content`、`chunks`、`chunkStrategy`、`charCount`、`wordCount`。
- 新增 `splitContentIntoChunks` 切分算法：支持自动分段（按结构/段落/标题）和自定义分段（分隔符、chunk 长度、重叠长度）。
- 更新 `retrieve.ts`：支持对上传文档的 chunks 进行检索，并从最佳 chunk 生成结构化答案。
- 重构 `AssistantAdminPage.tsx`：
  - 文档列表增加「来源」「分块/字符」列。
  - 顶部操作拆分为「上传文档」和「手动录入」。
  - 上传弹窗：拖拽上传、文件信息展示、原始内容在线编辑、切分策略配置、实时分块预览。
  - 编辑上传文档：编辑原始内容后自动重新切分。
  - 文档预览：上传文档为「原始内容 + 分块列表」双栏布局。
- 涉及文件：`src/prototypes/data-analysis-engine/data/assistantManual.ts`、`src/prototypes/data-analysis-engine/components/AIAssistant/retrieve.ts`、`src/prototypes/data-analysis-engine/pages/AssistantAdminPage.tsx`。
- tsc 检查无新增错误。

## AI 助手参数配置：补充模型调用参数

- 调研 Coze/Dify 模型调用参数：均支持 Temperature、Top P、Max Tokens、上下文轮数、响应格式、Presence Penalty、Frequency Penalty，Coze 还支持 top_k / response_format / sp_anti_leak。
- 扩展 `AssistantConfig` 数据模型：新增 `modelProvider`、`model`、`temperature`、`topP`、`maxTokens`、`contextRound`、`responseFormat`、`presencePenalty`、`frequencyPenalty`。
- 新增 `MODEL_PROVIDERS`（豆包、DeepSeek、Moonshot、OpenAI、Anthropic）与参数预设模板（精确/平衡/创意）。
- 重构 `AssistantAdminPage.tsx` 的「参数配置」页：
  - 新增「模型设置」：模型供应商、模型选择。
  - 新增「参数预设」：精确/平衡/创意一键切换。
  - 新增「模型调用参数」：Temperature、Top P、Max Tokens、上下文轮数、响应格式、Presence Penalty、Frequency Penalty。
- 涉及文件：`src/prototypes/data-analysis-engine/data/assistantManual.ts`、`src/prototypes/data-analysis-engine/pages/AssistantAdminPage.tsx`。
- tsc 检查无新增错误，dev server 53817/51720 运行正常。

## AI 助手头像：改为单图片上传

- 按用户反馈，将参数配置中的「头像 Emoji」改为图片上传，限制只能上传一张，再次上传即替换原图。
- 数据模型：`AssistantConfig.avatarEmoji` 改为 `avatarUrl`，存储图片 dataURL；未上传时回退显示默认机器人图标。
- 管理页：新增 `AvatarUpload` 组件，支持点击上传 / 拖拽上传、图片预览、删除/替换；页面标题头像同步显示上传图片。
- 悬浮面板/消息气泡：新增 `BotAvatar` 组件统一渲染，有 `avatarUrl` 时显示图片，否则显示渐变机器人图标。
- 涉及文件：`src/prototypes/data-analysis-engine/data/assistantManual.ts`、`src/prototypes/data-analysis-engine/pages/AssistantAdminPage.tsx`、`src/prototypes/data-analysis-engine/components/AIAssistant/AIAssistantWidget.tsx`。
- tsc 检查无新增错误。
