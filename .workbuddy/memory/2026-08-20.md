# 2026-08-20

## AI 助手对话记录与反馈模块

- 按用户反馈新增「对话记录」功能，用于管理员评估助手回答质量、调整知识库或模型策略。
- 设计 `ConversationLog` 数据模型，记录内容：
  - 基础：会话 ID、用户 ID/名称、提问时间、响应耗时
  - 内容：用户问题、助手回答文本、答案快照（标题/分类/摘要/步骤/路径）
  - 检索：是否命中、最佳匹配得分、召回文档列表（含得分与状态）
  - 模型：供应商、模型、Temperature、Top P、Max Tokens、上下文轮数、响应格式、惩罚系数
  - 反馈：点赞/点踩状态、反馈原因、修正答案
- 扩展 `retrieve.ts` 的 `AnswerResult`，增加 `retrieved`（召回文档+得分）与 `bestScore`，供日志调用。
- 更新 `AIAssistantWidget`：
  - 每次助手回复后自动写入对话记录
  - 消息气泡新增「这个回答有帮助吗？」点赞/点踩反馈按钮
  - 反馈状态同步回写 localStorage
- 更新 `AssistantAdminPage`：
  - 左侧导航新增「对话记录」Tab
  - 新增 `LogsTab`：搜索问题/回答/用户、按反馈状态筛选、列表展示时间/问题/命中/得分/反馈
  - 新增 `LogDetailDrawer`：展示完整问答、召回文档、模型参数、用户反馈

## AI 助手对话记录：移除清空按钮并内置示例记录

- 用户反馈对话记录需长期保留，移除「清空记录」按钮及对应 `clearConversationLogs` 调用。
- 在 `assistantManual.ts` 中新增 `SEED_LOGS` 内置示例记录：
  - 3 条达标记录：创建数据集、分享资产、数据大屏上线后查看（命中准确、反馈点赞/无反馈）
  - 3 条不达标记录：报销差旅费（未命中）、分享给外部用户（低分命中但缺少关键信息）、支持哪些数据库（答非所问）
- 修改 `getConversationLogs()`：内置示例记录始终保留，与用户真实记录合并并按时间倒序展示。
- 涉及文件：`src/prototypes/data-analysis-engine/data/assistantManual.ts`、`src/prototypes/data-analysis-engine/components/AIAssistant/retrieve.ts`、`src/prototypes/data-analysis-engine/components/AIAssistant/AIAssistantWidget.tsx`、`src/prototypes/data-analysis-engine/pages/AssistantAdminPage.tsx`。
- tsc 检查无新增错误，dev server 53817/51720 运行正常。

## 智能问数（NL2SQL）原型：方案确认并落地

- 用户确认 5 项决策：① 入口=AI 助手内「解答/问数」切换 + 独立「智能问数」页（防新用户不知有问数能力）；② 作用域=数据集问数 + 报表/仪表盘/大屏智能解读；③ 语义层全做（行业黑话同义词 / 指标口径 / 示例问数 / 数据集字段导入）；④ 图表=意图→类型自动映射 + 回答后手动切换；⑤ AI 管理整合进左侧「AI 中心」菜单（仅超管可见，由 Layout `NavGroup.adminOnly` 控制）。
- 新增数据层 `data/semanticLayer.ts`：黑话/指标/示例/数据集 Schema/作用域/问数记录类型与种子数据 + localStorage helpers（`getTerms/saveTerms/getMetrics/getExamples/getDatasets/getScopes/getQueryLogs/appendQueryLog` 等）。
- 新增问数引擎 `components/AIAssistant/queryEngine.ts`：`classifyIntent`→`pickDimension/pickMetric`→`buildMockData`→`buildSql`→`runQuery`；报表/仪表盘/大屏走智能解读分支（返回 metric 卡+摘要，sql 为注释）。
- 新增 `QueryResultView.tsx`：纯 SVG 图表渲染（line/bar/pie/metric/scatter/table）+ 推理过程 + SQL(深色 pre) + 手动切图(CHART_OPTIONS) + 结论 + 反馈。
- 新增独立页 `pages/QueryPage.tsx`：左侧作用域选择器+最近问数历史，右侧对话区（头部+内容+输入），`run()` 调 runQuery→setResult→appendQueryLog。
- 改造 `AIAssistantWidget.tsx`：新增「解答/问数」ModeTab、问数作用域选择器、问数结果渲染与图表切换/反馈；ChatMessage 扩展 `query/queryChartType/queryLogId`。
- 新增 `pages/SemanticLayerPage.tsx`（黑话/指标/示例/字段四子 Tab + 编辑抽屉 + 字段导入）、`pages/QueryLogsPage.tsx`（问数记录统计卡+搜索+反馈筛选+列表+详情抽屉）。
- 改造 `Layout.tsx`：数据分析组新增「智能问数」(Sparkles)；新增「AI 中心」组(adminOnly)含知识库/配置/语义层/对话记录/问数记录；`index.tsx` 新增 6 条路由 + `AI_TAB_DEFAULTS` 透传子 Tab。
- 涉及文件与实现要点见 `src/prototypes/data-analysis-engine/.spec/intelligent-query-plan.md` 第五、六章。
- tsc 检查 AI 相关文件无新增错误，dev server 53817/51720 运行正常。

## AI 中心菜单整合后的 UI 微调

- 左侧导航已直接提供「AI 中心」各子项入口，用户反馈 `AssistantAdminPage` 内部无需再保留左侧目录树。
- 改造 `AssistantAdminPage.tsx`：移除 `<aside>` 子导航与 `SubNavItem` 组件，移除未使用的 `BookOpen/Settings2/MessageSquare` import；内容区直接撑满整栏。
- 改造 `Layout.tsx`：账号下拉菜单中移除「AI 助手管理」按钮，移除未使用的 `Bot` import。
- tsc 检查无新增错误（既有 filter 类型错误为历史遗留，不在本次改动文件内），dev server 53817/51720 运行正常。

## AI 中心子菜单切换页面不同步修复

- 用户反馈：左侧「AI 中心」在「知识库文档」与「参数配置」之间切换时，右侧内容没有正确变化（实际显示的是 config 页）。
- 根因：`ai-knowledge` / `ai-config` / `ai-conv-logs` 都映射到同一个 `AssistantAdminPage` 组件，React 复用组件不会重置 `useState`，导致 `tab` 状态停留在首次进入的 Tab。
- 修复：`AssistantAdminPage.tsx` 中新增 `useEffect`，在 `defaultTab` 变化时同步 `tab` state。
- tsc 检查无新增错误。

## 智能问数：语义层/作用域接入系统真实资产

- 用户要求：问数的「训练数据」应建立在系统现有的数据集 / 报表 / 仪表盘 / 数据大屏上，而非脱离系统的假数据。
- `semanticLayer.ts` 改造：
  - 从 `data/mockData.ts` 导入真实 `datasets`(21) / `reports`(21) / `dashboards` / `dataScreens`。
  - 新增 `inferFields(name)`：按数据集名称业务域（订单/用户/商品/物流/财务/营销/访问日志/风控/审计/搜索/消息队列/地理等）推断默认字段语义（维度+指标+聚合），保证问数引擎可选出维度与指标；通用兜底字段。
  - `SEED_DATASETS` 改为由真实数据集生成（`id=DTxxx`、`tableName=dwd_${id}`、字段=inferFields）。
  - `SEED_SCOPES` 改为真实资产全集（数据集问数 + 报表/仪表盘/大屏智能解读）。
  - `SEED_EXAMPLES` / `SEED_QUERY_LOGS` 引用改为真实资产（DT001 订单明细数据集、DB001 运营核心指标等）。
  - `DATASETS_KEY` 升版为 `dae-semantic-datasets-v2`，避免旧 localStorage(ds-sales/ds-user) 与新作用域错配。
  - 新增 `SCOPE_TYPE_LABELS` / `SCOPE_TYPE_ORDER` / `groupScopes()` 供选择器按类型分组。
- `QueryPage.tsx`：左侧作用域列表按「数据集/报表/仪表盘/数据大屏」分组并可滚动。
- `AIAssistantWidget.tsx`：问数模式作用域由平铺 pill 改为按类型分组的 `<select>` 下拉（窄面板更可用）；移除不再使用的 Database/FileText/Gauge/Monitor 图标导入。
- tsc 检查无新增错误，dev server 53817 返回 200（HMR 已下发）。
