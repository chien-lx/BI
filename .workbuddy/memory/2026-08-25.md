# 2026-08-25

## 数据集预览弹窗移除「返回」按钮

- 用户反馈：数据集列表点击「预览」弹出的 `SchemaPanel` 右上角同时存在「关闭图标」和「返回」按钮，希望只保留关闭图标。
- 修改：
  - `src/prototypes/data-analysis-engine/components/SchemaPanel.tsx`：移除 `showBack` prop、移除 `ArrowLeft` import、移除头部「返回」按钮渲染。
  - `src/prototypes/data-analysis-engine/pages/DatasetPage.tsx`：调用 `SchemaPanel` 时去掉 `showBack` 属性。
- tsc 本次改动文件无新增错误。
